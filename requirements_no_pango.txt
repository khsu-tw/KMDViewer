# MDViewer - 完全不使用 Pango 的依賴列表
# 所有庫都是純 Python，無需系統庫

# GUI 框架 (PyQt5 - 使用 Qt5，不使用 Pango)
PyQt5==5.15.9
PyQt5-sip==12.13.0

# Markdown 處理 (純 Python)
markdown-it-py==3.0.0
mdit-py-plugins==0.4.0

# 程式碼高亮 (純 Python)
pygments==2.17.2

# PDF 匯出 (純 Python - 不使用 weasyprint/Pango)
reportlab==4.0.4

# 資料驗證
pydantic==2.5.3
